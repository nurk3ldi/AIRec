import styles from '../styles/Business.module.css'

/**
 * Бизнес — пусто, страница проектируется заново.
 *
 * Первая версия (карточка профиля 30/70, прайс-лист и график работы) снята
 * 2026-08-19 вместе со всеми компонентами `components/business/`; она есть в
 * истории git. Снята целиком, а не переделана по частям, по той же причине,
 * что и «Записи»: следующая раскладка не должна ничего наследовать от
 * предыдущей.
 *
 * Бэкенд не тронут и остаётся готовой частью — `/business`, `/business/services`
 * и `/business/working-hours` работают, а слой данных под ними (`lib/api.js`,
 * `lib/cities.js`, `lib/businessOptions.js`, `lib/schedule.js`) остался на
 * месте: экран перерисовывают, правила под ним — нет.
 *
 * Маршрут держим живым: пункт «Бизнес» есть в боковой панели, и пустая
 * страница честнее, чем ссылка, ведущая в 404.
 */
export default function BusinessPage() {
  return <div className={styles.page} aria-label="Бизнес" />
}
