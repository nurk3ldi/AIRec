import styles from '../styles/Home.module.css'

/**
 * Лендинг — пусто, страница проектируется заново.
 *
 * Здесь был герой: заголовок, абзац и две кнопки («Начать» / «Подробнее»);
 * он снят 2026-08-19 и есть в истории git. Снят целиком, а не переписан, по
 * той же причине, что и экраны панели: следующая раскладка не должна ничего
 * наследовать от предыдущей.
 *
 * `PublicLayout` по-прежнему даёт странице шапку с логотипом и кнопками входа,
 * так что путь к регистрации отсюда не теряется.
 */
export default function LandingPage() {
  return <main className={styles.page} aria-label="Главная страница" />
}
