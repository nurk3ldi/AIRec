import { useT } from '../lib/i18n'
import styles from '../styles/Assistant.module.css'

/**
 * Ассистент — пусто, страница проектируется заново.
 *
 * Раньше это был «Бизнес». Переименовано вместе с маршрутом: владелец приходит
 * сюда не «настроить компанию», а посмотреть, что знает и как отвечает его
 * ассистент — а услуги, цены и часы работы это ровно то, что ему нужно знать.
 * Слово выбрано не новое: в переписке подписи уже говорят «Ассистент», и баннер
 * в чате — «Ассистент остановлен».
 *
 * Первая версия страницы (карточка профиля 30/70, прайс-лист и график работы)
 * снята 2026-08-19 вместе со всеми компонентами `components/business/`; она
 * есть в истории git. Снята целиком, а не переделана по частям, по той же
 * причине, что и «Записи»: следующая раскладка не должна ничего наследовать от
 * предыдущей.
 *
 * **Бэкенд не переименован, и это не упущение.** `Business` — это сам салон:
 * часовой пояс, вместимость, услуги, часы работы. Ассистент их *читает*, но не
 * является ими, так что `/business`, `/business/services` и
 * `/business/working-hours` остаются как есть — вместе со слоем данных под ними
 * (`lib/api.js`, `lib/cities.js`, `lib/businessOptions.js`, `lib/schedule.js`).
 * Переименовали экран, а не сущность.
 *
 * Маршрут держим живым: пункт «Ассистент» есть в боковой панели, и пустая
 * страница честнее, чем ссылка, ведущая в 404.
 */
export default function AssistantPage() {
  const t = useT()

  return <div className={styles.page} aria-label={t('nav.assistant')} />
}
