# Direct dependencies, pinned to the versions this project was verified against.
# Transitive packages are resolved by pip.
fastapi==0.141.1
uvicorn[standard]==0.52.1
SQLAlchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.19.1
pydantic==2.13.4
pydantic-settings==2.15.0
email-validator==2.3.0
argon2-cffi==25.1.0
PyJWT==2.13.0
python-multipart==0.0.32
Pillow==12.3.0
# Windows ships no IANA time zone database, so `zoneinfo` has nothing to read
# and every slot calculation would raise ZoneInfoNotFoundError. Required, not
# optional: booking is done entirely in the business's own zone.
tzdata==2026.3
# The WhatsApp Cloud API is HTTP and nothing in the stdlib speaks it without
# blocking the event loop — `urllib` in an `async def` is the same mistake
# hashing a password inline would be. Pinned, and used only by
# `app/core/whatsapp.py`.
httpx==0.28.1
